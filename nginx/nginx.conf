events {}

http {
    upstream backend {
        server 172.17.0.1:8001 weight=9;
        server 172.17.0.1:8002 weight=1;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}
